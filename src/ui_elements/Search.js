const Search = ({
  searchInput,
  setSearchInput,
  setSearchSuggestions,
  searchSuggestions,
  handleClick,
  handleSuggestionClick,
  handleInputChange
}) => {
  return <div className={'center'}></div>
}

export default Search
